import { FC } from 'react'

const EndContent: FC = ({ children }) => {
  return <div className="font-grenze prose mb-20 text-center text-2xl font-light text-white">{children}</div>
}

export default EndContent
